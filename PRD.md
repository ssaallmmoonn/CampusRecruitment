# 基于协同过滤的校园招聘系统 - 需求分析文档

## 一、文档概述

### 1.1 文档目的

明确「基于协同过滤的校园招聘系统」的功能、非功能需求，为毕业设计开发提供清晰的开发边界和验收标准，参考智联校园招聘（https://xiaoyuan.zhaopin.com/）核心场景，适配高校毕设的开发复杂度。

### 1.2 系统定位

面向在校大学生、企业 HR、学校管理员的校园招聘平台，核心特色是基于**协同过滤算法**为学生智能推荐匹配的职位，解决校园招聘中 “学生找职位效率低、企业招不到合适人选” 的痛点。

### 1.3 核心角色

|    角色    |                           核心诉求                           |
| :--------: | :----------------------------------------------------------: |
|  学生用户  | 注册登录、完善简历、浏览 / 搜索 / 收藏 / 投递职位、查看个性化推荐职位 |
|  企业用户  |    注册登录、发布职位、管理职位、查看投递简历、筛选候选人    |
| 系统管理员 | 审核企业资质、管控职位内容、管理用户账号、查看系统数据（如职位 / 用户统计） |

记录：超级管理员账号密码http://localhost:8000/admin
- 用户名： admin
- 密码： password123
- 邮箱： admin@example.com

### 1.4 技术栈

| 模块            | 推荐技术                        |      |
| --------------- | ------------------------------- | ---- |
| 前端            | Vue3 + Element Plus + Axios     |      |
| 后端            | Python + Django Rest Framework  |      |
| 数据库          | MySQL 8.0                       |      |
| 协同过滤算法    | Python（Pandas + Scikit-learn） |      |
| 部署 / 开发工具 | Docker（可选） + Git + Postman  |      |

特别说明一下协同过滤算法：

协同过滤算法：Python + Pandas + Scikit-learn。

这是毕设的核心，从零实现协同过滤无需复杂框架，重点是**落地简单、逻辑清晰**：

- 核心库：
    - Pandas：处理数据（如用户 - 职位行为数据的清洗、转换）；
    - Scikit-learn：提供基础的相似度计算（余弦相似度、皮尔逊相关系数），无需手写算法底层；
    - Numpy：辅助数值计算（可选，Pandas 已封装大部分功能）。
- 算法选型（适合毕设）：
    - 先实现**基于用户的协同过滤（UserCF）**：找到和目标学生行为相似的其他学生，推荐这些学生喜欢的职位；
    - 再补充**基于物品的协同过滤（ItemCF）**：找到目标学生浏览 / 投递过的职位相似的其他职位，作为推荐结果；
    - 理由：这两种算法逻辑简单，代码易实现，且能直观展示推荐效果（适合毕设答辩讲解）。
- 算法落地步骤（简化版）：
    1. 从 MySQL 读取学生 - 职位行为数据（如浏览、收藏记录）；
    2. 构建用户 - 职位评分矩阵（行为类型量化：浏览 = 1，收藏 = 3，投递 = 5，无行为 = 0）；
    3. 计算用户相似度（UserCF）或职位相似度（ItemCF）；
    4. 根据相似度筛选 Top-N 推荐职位，返回给后端接口。

## 二、功能需求

### 2.1 通用模块（所有角色）

#### 2.1.1 登录 / 注册

- 注册：

    - 学生：手机号 / 学号注册，验证短信 / 邮箱，设置密码，完善基础信息（姓名、专业、学历、毕业时间）；
    - 企业：企业名称、统一社会信用代码、联系人、手机号，上传营业执照（管理员审核后激活账号）；
    - 管理员：仅后台创建，不开放注册。

    

- 登录：

    - 支持账号密码登录，记住密码、忘记密码（短信 / 邮箱找回）；
    - 登录后根据角色跳转对应首页（学生首页 / 企业首页 / 管理员后台）。

    

- **退出登录**：清除登录状态，返回首页。

#### 2.1.2 个人中心

- 基本信息修改（头像、联系方式、密码等）；
- 角色专属信息管理（学生：简历管理；企业：企业信息完善；管理员：无额外信息）。

### 2.2 学生端核心功能（核心）

#### 2.2.1 职位浏览与搜索

- 参考智联校园招聘的职位列表页，展示职位名称、企业名称、薪资、地点、发布时间；
- 筛选功能：按行业、薪资、地点、学历要求、发布时间筛选；
- 搜索功能：支持职位名称、企业名称模糊搜索；
- 职位详情页：展示职位描述、任职要求、企业介绍，提供「收藏」「投递简历」按钮。

#### 2.2.2 简历管理

- 简历创建：填写基本信息（姓名、专业、学历）、实习 / 项目经历、技能证书、求职意向（期望职位、薪资、地点）；
- 简历编辑 / 删除：支持多份简历（如实习版、校招版），设置默认简历；
- 简历投递记录：查看投递的职位、投递时间、企业查看状态（已查看 / 未查看）。

#### 2.2.3 个性化推荐（核心亮点）

- 推荐专区：首页显著位置展示「为你推荐」职位列表（基于协同过滤算法）；
- 推荐逻辑：基于学生的「浏览 / 收藏 / 投递」行为，计算相似用户 / 相似职位，推荐 Top10 职位；
- 刷新推荐：支持手动刷新推荐列表，更新推荐结果。

#### 2.2.4 行为记录

- 查看历史浏览的职位、收藏的职位；
- 支持删除收藏 / 清空浏览记录。

### 2.3 企业端核心功能

#### 2.3.1 职位管理

- 职位发布：填写职位名称、薪资、地点、学历要求、职位描述、任职要求，提交后需管理员审核；
- 职位列表：查看已发布 / 待审核 / 已驳回的职位，支持编辑、下架、删除；
- 职位数据：查看职位的浏览量、收藏量、投递量。

#### 2.3.2 简历管理

- 投递列表：查看投递本企业职位的学生简历，支持「查看详情」「标记已读 / 有意向 / 拒绝」；
- 简历筛选：按专业、学历、求职意向等筛选简历；
- 简历导出：支持导出简历为 Excel 格式。

#### 2.3.3 企业信息管理

- 完善企业简介、行业、规模、办公地点等信息；
- 上传企业 logo、环境图片。

#### 2.3.4 企业数据看板

- 1.核心看板（实时概览）

    这部分通常放在页面顶部，用大数字或仪表盘显示，让HR一眼看到目前的招聘“底盘”。

    - **累计指标：** 在招职位数、累计收到的简历总数、待处理简历数。
    - **活跃指标：** 今日新增投递、今日新增浏览。

- 2.职位效果分析（漏斗模型）

    这是数据统计的核心。你可以通过这三个指标计算出**招聘漏斗**，分析职位的吸引力。

    - **转化率分析：**
        - **兴趣率：** $收藏量 / 浏览量$（反映职位描述是否吸引人）。
        - **投递率：** $投递量 / 浏览量$（反映职位要求与学生匹配度是否合理）。
    - **热门职位排行：** 展示投递量最高的前 10 个职位，帮助企业了解哪些岗位是校园热门。
    - **冷门职位预警：** 浏览量高但投递量极低的职位（可能需要修改任职要求）。

- 3.协同过滤算法相关数据（算法反馈）

    既然你的选题特色是**协同过滤**，企业端应该能看到算法带来的收益。

    - **推荐点击率：** 统计有多少投递是通过系统“猜你喜欢”或“智能推荐”入口进来的。
    - **人才匹配度分布：** 展示投递学生中，高匹配度（算法评分 > 80分）的人数占比。这能向企业证明你的系统推荐确实有效。

- 4.学生画像（用户属性分析）

    帮助企业了解关注他们的是一群什么样的学生。

    - **专业分布：** 饼图展示投递学生的专业背景（如：计算机、电子、金融等）。
    - **学历/年级分布：** 统计本科与硕士的比例。
    - **技能关键词云：** 提取投递简历中的高频技能词（如：Java, Vue, Python），让企业知道这一届学生的技能栈偏好。

在撰写文档时，你可以强调：**“本系统通过监控浏览与投递的转化比率，为企业提供招聘决策支持，并利用协同过滤算法提升了长尾职位（冷门职位）的曝光度。”**





### 2.4 管理员端核心功能

#### 2.4.1 权限管控

- 企业审核：审核企业注册信息，通过 / 驳回（填写驳回理由）；
- 职位审核：审核企业发布的职位，过滤违规内容（如虚假薪资、违法岗位）；
- 用户管理：查看所有用户，禁用 / 启用账号，删除违规用户。

#### 2.4.2 数据统计

- 1.平台规模与增长（核心指标）：
    - 学生总数；
    - 企业总数；
    - 职位总数；
    - 简历投递总数。


- 2. 活跃度与粘性分析（流量监控）

    通过你设置的浏览、收藏、投递数据，计算全平台的活跃情况。

    - **DAU/MAU：** 每日/每月活跃用户数（学生和企业双端）。
    - **交互热度：** 全平台每日产生的总浏览量、总收藏量、总投递量趋势图。
  
- 3. 协同过滤算法效果监控（毕设核心亮点）

    这是证明你算法价值的关键数据。

    - **算法覆盖率：** 成功计算出推荐列表的用户占总用户数的比例。
    - **推荐转化贡献：**  
        - **计算公式：** $通过推荐位产生的投递量 / 全平台总投递量$。
        - **意义：** 如果这个比例高，说明你的协同过滤算法真正促进了双向匹配。

- 4.招聘生态平衡性（分布分析）

    防止平台出现“马太效应”（即头部企业简历爆满，小企业无人问津）。

    - **职位类别分布：** 哪些技术方向（如前端、后端、算法）的职位最多。
    - **投递热力图：** 统计不同时间段的投递频率，分析学生找工作的高峰期。
    - **供需比分析：**  
        - **最紧缺职位：** 浏览量高但投递量极低的类别。
        - **竞争最激烈职位：** 投递量/职位数 比例最高的类别。

##### 建议的数据统计页面布局（未采用）

针对管理员后台，建议采用“**总-分**”结构：

**A. 顶部：数字战报 (Data Cards)**

- 用卡片形式展示：用户总数、今日新增、待办审核数。

**B. 中部：趋势折线图 (Trend Charts)**

- 展示过去 30 天内，学生活跃度与企业发布职位量的对比，观察供需趋势。

**C. 底部：分布饼图与排行榜 (Distribution & Rankings)**

- **左侧饼图：** 企业行业分布、学生专业背景分布。
- **右侧列表：** 活跃度最高的前 10 家企业、算法推荐点击率最高的 Top 职位。







### 2.5 算法模块（后端核心）

#### 2.5.1 数据采集

- 采集学生的行为数据（浏览、收藏、投递），量化为评分（如：浏览 = 1，收藏 = 3，投递 = 5）；
- 构建「用户 - 职位」评分矩阵（无行为则为 0）。

#### 2.5.2 协同过滤实现

- 基于用户的协同过滤（UserCF）：计算目标学生与其他学生的相似度（余弦相似度），推荐相似学生高评分的职位；
- 基于物品的协同过滤（ItemCF）：计算目标学生已交互职位与其他职位的相似度，推荐高相似度职位；
- 算法接口：封装为 FastAPI 接口（如`/api/recommend/{student_id}`），返回推荐职位列表（含职位 ID、推荐得分）。

#### 2.5.3 算法优化（可选，提升毕设质量）

- 缓存推荐结果：用 Redis 缓存推荐结果，避免重复计算（过期时间 1 小时）；
- 冷启动处理：对无行为记录的新学生，推荐热门职位（按浏览量 / 投递量排序）。

## 三、非功能需求

### 3.1 性能需求

- 页面加载时间≤3 秒；
- 推荐接口响应时间≤1 秒（算法计算简化，优先保证速度）；
- 支持至少 1000 条职位数据、500 个用户数据的正常查询。

### 3.2 兼容性需求

- 前端适配 PC 端主流浏览器（Chrome、Edge、Firefox）；
- 无需适配移动端（毕设聚焦核心功能，降低复杂度）。

### 3.3 安全性需求

- 密码加密：用户密码采用 BCrypt 加密存储，不明文保存；
- 接口鉴权：所有后端接口验证登录状态（FastAPI 用 JWT 令牌）；
- 防 SQL 注入：使用 ORM 框架（SQLAlchemy），避免原生 SQL 拼接。

### 3.4 易用性需求

- 界面参考智联校园招聘，简洁清晰，操作流程无冗余步骤；
- 关键操作（如投递简历、发布职位）给出明确的成功 / 失败提示。



## 四、数据字典（核心表）

| 表名      | 字段名        | 类型         | 说明                                     |
| --------- | ------------- | ------------ | ---------------------------------------- |
| user      | id            | int(11)      | 主键，自增                               |
|           | username      | varchar(50)  | 账号（手机号 / 学号）                    |
|           | password      | varchar(100) | 加密后的密码                             |
|           | role          | tinyint(1)   | 角色：1 - 学生，2 - 企业，3 - 管理员     |
|           | status        | tinyint(1)   | 状态：0 - 禁用，1 - 正常                 |
|           | create_time   | datetime     | 创建时间                                 |
| student   | id            | int(11)      | 主键（关联 user.id）                     |
|           | name          | varchar(20)  | 学生姓名                                 |
|           | major         | varchar(50)  | 专业                                     |
|           | education     | varchar(20)  | 学历（本科 / 硕士）                      |
| company   | id            | int(11)      | 主键（关联 user.id）                     |
|           | company_name  | varchar(100) | 企业名称                                 |
|           | credit_code   | varchar(50)  | 统一社会信用代码                         |
|           | audit_status  | tinyint(1)   | 审核状态：0 - 待审核，1 - 通过，2 - 驳回 |
| job       | id            | int(11)      | 主键                                     |
|           | company_id    | int(11)      | 关联企业 ID                              |
|           | job_name      | varchar(50)  | 职位名称                                 |
|           | salary        | varchar(20)  | 薪资（如 8k-12k）                        |
|           | location      | varchar(50)  | 工作地点                                 |
|           | audit_status  | tinyint(1)   | 职位审核状态                             |
| behavior  | id            | int(11)      | 主键                                     |
|           | student_id    | int(11)      | 学生 ID                                  |
|           | job_id        | int(11)      | 职位 ID                                  |
|           | behavior_type | tinyint(1)   | 行为类型：1 - 浏览，2 - 收藏，3 - 投递   |
|           | create_time   | datetime     | 行为时间                                 |
| resume    | id            | int(11)      | 主键                                     |
|           | student_id    | int(11)      | 学生 ID                                  |
|           | resume_name   | varchar(50)  | 简历名称                                 |
|           | content       | text         | 简历内容（JSON 格式）                    |
| recommend | id            | int(11)      | 主键                                     |
|           | student_id    | int(11)      | 学生 ID                                  |
|           | job_id        | int(11)      | 职位 ID                                  |
|           | score         | float        | 推荐得分（0-1）                          |
|           | create_time   | datetime     | 推荐时间                                 |



## 五、页面结构参考（对标智联校园招聘）

1. 首页：轮播图（校园招聘公告）、热门职位、推荐职位、行业分类；
2. 学生登录 / 注册页：简洁表单，适配学生注册流程；
3. 学生首页：推荐职位专区、职位搜索 / 筛选、个人中心入口；
4. 职位详情页：职位信息、企业信息、投递 / 收藏按钮；
5. 企业首页：职位发布入口、投递简历列表、数据统计；
6. 管理员后台：审核列表、数据统计图表、用户管理列表。

### 总结

1. 核心功能聚焦「学生端个性化推荐」，这是毕设的核心亮点，企业端 / 管理员端满足基础 CRUD 即可；
2. 算法部分优先实现**基于用户 / 物品的协同过滤**，逻辑简化（用 Scikit-learn 计算相似度），无需复杂优化；
3. 技术栈落地优先级：先打通前后端基础功能（登录、职位 CRUD），再集成算法实现推荐，最后补充统计 / 优化功能。

部署服务器：
root
Yyys241001